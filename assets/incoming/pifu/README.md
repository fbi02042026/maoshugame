# 仓鼠皮肤 · 未改名原图（可选）

若文件名还是中文（如 `皮肤1.png`），放这里，运行 `python tools\import_skins.py` 会重命名并写入 `assets\skins\hamster\`。

---

**若已改成 `hamster_skin_01.png` 等规范名，请直接放：**

```
assets\skins\hamster\
```

然后同样运行 `python tools\import_skins.py` 即可（会扫描 hamster 目录并生成 `skin_catalog.json`）。
